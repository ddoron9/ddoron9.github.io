- In Slack #ops on 2026-03-25, Doyi defined a new `manager` bot concept for OpenClaw.
- Manager scope is limited to research and video follow-up work; it activates only when Doyi explicitly turns it on for a bounded number of hours.
- Manager should take over follow-up replies after doyiclaw's initial answer, and report to the channel directly rather than thread-only.
- Safety policy clarified: external send is allowed only if payload contains no token/key/personal info; GitHub push is allowed with secret-safety checks; downloads are restricted to libraries/packages/model weights/checkpoints, not PDFs or user documents.
- Created reusable artifacts in `openclaw-personal-ops/`: `reports/2026-03-25_manager_mvp_spec.md`, `prompts/manager_delegate.md`, and `state/manager/policy.json`.
